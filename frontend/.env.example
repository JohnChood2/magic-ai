# Where the FastAPI backend lives. Used server-side by the /api/chat proxy.
BACKEND_URL=http://localhost:8000

# Optional: surface backend URL in the browser (not needed if we always proxy).
# NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
