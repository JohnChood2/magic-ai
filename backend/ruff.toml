target-version = "py312"
line-length = 100

[lint]
select = [
  "E",   # pycodestyle errors
  "F",   # pyflakes
  "I",   # isort
  "B",   # bugbear
  "UP",  # pyupgrade
  "W",   # warnings
]
ignore = [
  "E501",  # line length handled by formatter
]

[lint.per-file-ignores]
"tests/*" = ["B", "S"]
